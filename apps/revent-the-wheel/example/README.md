# Example (Re-event the Wheel)

This is a scaffold project demonstrating the required `lessons/` folder pattern.

When you start a real project, copy this folder and add your app source under `src/`.
